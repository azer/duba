
      USAGE

          duba [command]

      COMMANDS

         pull     Pull specified project from remote workspace. e.g duba pull foobar.js
         push     Push specified project into remote workspace. e.g duba push
         run      Run given command in the remote project directory. e.g duba run head package.json
         setup    Setup a new Duba configuration on ~/.duba
